main: '${main}'
prefix: '${name}'
name: '${name}'
version: '${version}'
description: '${description}'
api-version: '1.16'
authors: [ "${authors}" ]
website: "${url}"
softdepend: [ ]
depend: [ ]
loadbefore: [ ]
commands:
  kptreload:
  kpt:
